// check that there is a back to list of stock button that redirects to correct url
// check that there is the stock title and symbol corresponding to url
// check that it display month name + change + mspr + arrow for each month
// check that it display no data for other months for months with no data
// maybe intercept or mock ?
